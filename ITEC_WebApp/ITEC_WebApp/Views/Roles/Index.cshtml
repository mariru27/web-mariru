@model IEnumerable<ITEC_WebApp.Models.Role>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameRole)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameRole)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRole">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRole">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRole">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
