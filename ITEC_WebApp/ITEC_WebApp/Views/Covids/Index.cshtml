@model IEnumerable<ITEC_WebApp.Models.Covid>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProcentageVaccination)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Country)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProcentageVaccination)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Country.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCovid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCovid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCovid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
